# Реставрация объектов — портфолио

Премиальная презентация 16:9 для инвесторов. Сделана по workflow скилла **cjl-slides**,
стиль **№18 Luxury Fashion House** в тёплой ivory/графитовой адаптации.

## Файлы

| Файл | Что это |
|---|---|
| `restavraciya-obektov-Luxury.html` | Готовая презентация. Один самодостаточный файл: шрифты и фото вшиты, интернет не нужен. Листание — стрелки ← → на клавиатуре, на тач-устройствах — свайп. |
| `restavraciya-obektov-Luxury.pptx` | Редактируемая версия для PowerPoint (13.333 × 7.5 дюйма). |

## Структура — 14 слайдов

1. Обложка
2–3. Гиляровского, 37к2 — ДО / ПОСЛЕ
4–5. Площадь Борьбы, 13А — ДО / ПОСЛЕ
6–7. Мясницкая, 13 стр. 2 — ДО / ПОСЛЕ
8–9. Николоямская, 51 стр. 2 — ДО / ПОСЛЕ
10–11. Окружной проезд, 16 и 16 стр. 2 — ДО / ПОСЛЕ
12–13. Большая Почтовая, 18/20 стр. 15 — ДО / ПОСЛЕ
14. Финал — контактный лист портфолио

Слайды ДО и ПОСЛЕ одного объекта построены на одной сетке: фотография занимает 76 %
площади и стоит в одинаковой рамке, название объекта — на одной и той же высоте.
Меняются только маркировка и блок характеристик. Сторона фотографии чередуется по объектам.

Блок характеристик (Площадь здания / Этажность / Год постройки / Состояние / Статус /
Коммуникации) заполнен только названиями — значения оставлены пустыми.

## Замена фотографий

### В PPTX
Правый клик по изображению → **«Изменить рисунок»**. Кадрирование и положение сохраняются.
В панели выделения (Главная → Выделить → Область выделения) слоты подписаны:
`ФОТО-ПЛЕЙСХОЛДЕР — okruzhnoy-after` и т. п.

### В HTML
Найти нужный `data-slot` и заменить

```html
<div class="frame ph" data-slot="okruzhnoy-after"> ... </div>
```

на

```html
<div class="frame" data-slot="okruzhnoy-after" style="--img:url('фото.jpg')"></div>
```

(убрать класс `ph` и внутренний блок, задать `--img`).

### Пересборка обоих файлов сразу
Положить фото в `assets/photos/`, добавить строку в `PHOTOS` в `build/deck_spec.py`:

```python
"okruzhnoy-after": "assets/photos/okruzhnoy_after.jpg",
```

и выполнить:

```bash
python3 build/build_html.py       # HTML
python3 build/extract_layout.py   # снять геометрию
python3 build/gen_ph.py           # перерисовать оставшиеся плейсхолдеры
python3 build/build_pptx.py       # PPTX
```

## Слоты фотографий

| Слот | Где |
|---|---|
| `cover` | обложка |
| `giliarovskogo-before` / `-after` | Гиляровского, 37к2 |
| `borby-before` / `-after` | Площадь Борьбы, 13А |
| `myasnickaya-before` / `-after` | Мясницкая, 13 стр. 2 |
| `nikoloyamskaya-before` / `-after` | Николоямская, 51 стр. 2 |
| `okruzhnoy-before` / `-after` | Окружной проезд, 16 и 16 стр. 2 |
| `pochtovaya-before` / `-after` | Большая Почтовая, 18/20 стр. 15 |
| `final-<объект>` | плитки финального слайда |

## Что уже стоит из реальных фотографий

С Google Drive («Фото для презентации») удалось прочитать одну папку объектов —
**«Большая почтовая 18/20 стр15»**. Оттуда взята фотография фасада: она стоит на слайде
«Большая Почтовая — ДО реставрации» и, с затемнением, на обложке.

Остальные слоты — оформленные плейсхолдеры в стиле презентации.

## Шрифты

Cormorant Garamond Light (заголовки) и Noto Sans (маркировки) — по белому списку скилла.
В HTML они вшиты в файл. Для точного отображения PPTX эти шрифты стоит установить на
машине, где презентация будет открываться, иначе PowerPoint подставит замену.
